platform: windows

inputs:
  - name: getip 
  
run:
  dir: getip
  path: powershell
  args: ["Start-Sleep -s 180"]
  #args: ["c:\\progra~1\\Git\\bin\\git.exe clone https://github.com/OpusDude/cc-win-worker"]

run:
  path: powershell
  args: ["Get-NetIPAddress | Format-Table"]
