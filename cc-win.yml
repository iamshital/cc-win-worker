platform: windows

inputs:
  - name: AutomationCode
run:
  dir: AutomationCode
  path: powershell
  args: ["Copy-Item -Path C:\concourse-worker\common-files\* -Destination . -Recurse -Verbose -Force; dir;"]
